# Copy to .dev.vars for `npm run dev`, then fill values only when deliberately
# enabling the backend technical preview. The repository defaults stay inert.
DEVICE_TOKEN_SECRET=
ADMIN_TOKEN=
INVITE_CODES=
ARK_API_KEY=
OPENSPEECH_API_KEY=
