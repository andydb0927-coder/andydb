# 风格系统真实化验收

## 范围

起点：`076e40a7566658e998229f4cad4a908235f6aa3a`，分支 `codex/platform-shell-phase`。
保留开始时已存在的 7 张修改中截图；不改密钥文件，不操作用户浏览器数据，不调用真实 API。

## 已实现

| 功能 | 实现与数据来源 |
| --- | --- |
| 风格声明 | 原有六张示例卡迁入共享目录，增加提示词片段、适用类型、可选 provider ID 限制。属于本地提示词模板，不宣称训练模型。 |
| 生成接线 | 图片、视频的实际请求 prompt 前置片段；文本/脚本常规生成使用豆包 system 消息，保留原 JSON/创作约束。原始提示词不被覆写。 |
| 收藏与最近使用 | IndexedDB `stylePreferences`，重开、刷新保留；失败提示可见，不伪报成功。 |
| 自定义卡 | IndexedDB `styles`；名称、片段必填；可选 PNG/JPEG/WebP 本地封面≤2MB；支持选择、收藏。AI训练禁用并提示“待接入风格训练服务”。 |
| 应用与兼容性 | 图片、视频、文本、脚本常规生成区均有入口；选中卡高亮，可移除。不兼容模型/类型在 UI 和注册表双重拦截。 |
| 摘要与恢复 | composer、图片确认、历史记录与重发确认显示风格名；节点与任务持久化独立快照。历史重发/失败重试使用原快照，移除风格不复活旧配置。 |
| 数据兼容 | 数据库新增第11版两张表；节点可选字段不影响旧项目；工作流JSON保留并校验风格，撤销可恢复选择。 |

## 测试与证据

- TDD：先运行契约测试、组件测试，分别得到缺少风格模型/选择器模块的失败结果；实现后修复类型边界、旧状态残留及新增视频入口断言。
- Typecheck：通过。
- Vitest：170 文件、1431/1431 通过（原168文件、1412项；新增19项）。
- `build:mock`：通过，生产产物仍是未配置真实模型的禁用态。
- Playwright：107/107 通过（原104项；新增3项），退出码0，耗时4.4分钟。
- 新增 E2E 三项：图片自定义+收藏+刷新+移除后历史重发、721px视频风格与不兼容项、文本真实请求的system前缀。
- 日志与截图：`docs/qa/evidence/style-system/`。所有生成使用伪密钥和拦截 fixture，仅验证请求契约与界面，不等同于付费实测。
- 安全扫描：跟踪/待提交文本与 mock bundle 未发现高置信度密钥匹配或非空凭证字段；没有读取 `.env.local`。扫描非完整安全审计。

## 非阻断事项与使用方法

单测仍有既有 localhost:3000 的 EPERM 环境日志，构建仍提示大 chunk，均保留诊断；提交日志只规范行尾空白与末尾空行，不修改计数或错误内容，未格式化输出备份于本轮临时目录。测试失败数为0。Playwright 使用隔离4189端口及全新上下文，不清除用户 IndexedDB。

全量 E2E 日志在图片切片/媒体导入用例附近出现一次 `ResizeObserver loop completed with undelivered notifications`，未导致测试失败，不能据此声称全站控制台零错误。针对这两项已有操作另行复跑：2/2通过（9.6秒），未再次出现该诊断；保留 `media-diagnostic-recheck.log`，未以屏蔽异常、放宽断言或增加重试掩盖。新增风格图片流程的 pageerror 收集为空。该偶发诊断尚未定位根因，作为后续稳定性观察项记录，不宣称已修复。

操作：选节点→“风格”→查看片段及兼容类型→“应用”；也可“自定义风格”保存后应用。composer显示风格名后按原流程生成。线上未配置 API 时仍不能付费生成；本批未改变该安全门。

回滚：可回滚本批 UI 与生成接线，但须保留第11版数据库声明；已升级的 IndexedDB 不做降版或删表。旧项目字段仍可读取，禁止为回滚清空用户数据库。
