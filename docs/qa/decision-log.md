# 稳定化决策日志

| 编号 | 决策 | 理由/限制 |
| --- | --- | --- |
| DEC-01 | 起始HEAD为f1e098d；只在codex/current-product-stabilization改动 | 不把历史未推送提交误当新工作；不触发部署工作流。 |
| DEC-02 | 采用计划中11个文件与A–I报告结构 | 用户未指定完整文件名/字母标题；以最小完整交付补齐，不阻塞询问。 |
| DEC-03 | 浏览器测试用隔离上下文，真实API请求拦截，生产页不访问 | 保护用户数据与费用，API仅核查UI状态。 |
| DEC-04 | 旧审计为历史快照，增加勘误而非删除历史 | 避免把早期缺口误判为当前BUG；统一以新功能CSV为准。 |
| DEC-05 | 不改变API适配器和数据库schema | 本阶段只处理UI稳定化；五条真实链路及后处理代码均冻结。 |
| DEC-06 | preview采用Vite的isPreview分支 | 根因是预览服务器base与构建base不一致；不通过复制目录或硬编码资源链接补洞。 |
| DEC-07 | 同URL新建仅合并进行中的请求 | 避免双击重复项目，同时允许稍后再次创建、不同模板创建及保存失败后重试。 |
| DEC-08 | 缩放使用等效CSS布局并披露限制 | 自动化可确定性复验；不能据此宣称原生浏览器缩放或Quark全覆盖。 |
| DEC-09 | 通知原始偶发超时暂列P3观察 | 首次失败后原样单跑3次和并发12次通过；未证实产品根因，不删测试、不盲加延迟、不声称根治。 |
| DEC-10 | 视频空输入只修UI资格与列表发出的动作 | 复用已有generate校验，避免改动provider/网络/费用协议；保留“重生成”及加入时间线入口。 |
| DEC-11 | 明确待接入及本地示例不作为BUG | 状态CSV单列边界；不在稳定化阶段接新API或补全尚未实现的业务。 |
| DEC-12 | 不自动推送新分支 | 本次只要求隔离稳定化且不部署；本地提交可回滚，避免历史发布workflow被触发。 |
| DEC-13 | 缩放控件用React Flow公开主题变量 | 实机及失败测试确认图标对比度仅1.06:1；通过第三方正式主题入口修复导入顺序覆盖，不堆叠选择器或!important。 |
| DEC-14 | Controls显式top-right并移除冲突的基础定位 | 默认bottom-left与项目right同时生效导致横向拉伸并被小地图盖住；使用组件position属性，不提高z-index压过其他按钮。 |
| DEC-15 | 视图栏与引导区共用避让尺寸 | 窄屏引导卡底边原只避让工具坞，覆盖了上移的小地图。共用底偏移和高度计算；缩放条采用内容固有宽度，保留窄屏原有侧向定位。 |
| DEC-16 | 多结果网格退出固定高媒体框，作为同级正常流内容 | DOM有4图不代表可操作；截图确认网格被composer盖住。只调整ImageResults挂载层级，让composer按完整内容底边定位；不堆高度补丁、不改资产/版本模型。 |
| DEC-17 | 原始工具日志保留字节，不清洗空白或告警 | 暂存后diff空白检查仅在生成的日志/error-context中发现尾随空格或末尾空行；保留证据，代码和人工文档单独执行diff --check。四道质量门禁不减项。 |
