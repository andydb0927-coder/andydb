# 项目永久质量门禁（10条）

1. **先确认工作树。** 记录分支、HEAD和已有改动；在用户指定分支工作。保留他人的代码、截图与数据，不用reset/clean掩盖现场。
2. **保护凭据。** 不读取或输出本机密钥；不提交`.env.local`、`.env.*.local`及真实Key。构建产物和测试日志也要检查；线上与QA只能显式mock构建。
3. **隔离测试。** 浏览器用新BrowserContext和独立测试端口，不清用户IndexedDB/localStorage。真实生成必须单独授权；自动化一律fixture加外网拦截。
4. **先红后绿。** 缺陷先保留复现、失败断言和必要截图，再修根因；不删除失败测试、不放宽断言掩盖产品错误、不以重试次数伪造稳定。
5. **四道门禁有序运行。** `cd app`后依次typecheck→test:run→build:mock→`PLAYWRIGHT_OFFLINE_DIST=dist npx playwright test`。保留实际计数、退出码与日志；未跑不可称全绿。
6. **保护交互契约。** 1440/1280/1024主验、800/721及200%等效缩放可达、390不白屏；输入与弹窗内快捷键不得误触画布；保留按钮名称、焦点回归与禁用原因。
7. **最小根因修复。** 不用`any`、吞异常、关闭类型检查、隐藏功能、叠加`!important`或删功能来过测试。新增错误必须有安全、可恢复的中文反馈。
8. **兼容持久数据。** 节点、连线、版本、资产、历史和多画布变更要向后兼容；覆盖刷新恢复、项目隔离、并发点击和失败重试；删除有影响时先确认。
9. **诚实表达能力。** REAL_LOCAL/LIVE_API/MOCK/STATIC/PARTIAL/PLACEHOLDER/BROKEN/NOT_IMPLEMENTED要有源码依据；未配置或待接入不算BUG；fixture通过不等于真实供应商验收通过。
10. **可回滚交付。** 修改前后截图与回归测试对应BUG台账；提交前diff检查、凭据扫描、恢复测试覆盖的原有证据。只在授权时推送或部署，并以实际执行结果报告，不靠远程跟踪状态猜测。
