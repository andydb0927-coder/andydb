# CanvasWorkspaceChrome 组件规格

目标文件：`src/features/canvas/CanvasTopBar.tsx`、相关测试、`src/styles/global.css`。

- 来源状态：LibLib 画布为登录态，本次不读取其项目内容和内部交互。本地无限画布是产品核心，不重构工作流。
- 只统一顶部标题、保存/同步状态、预览/导出等主动作的视觉优先级；节点、边、快捷键和生成路径契约不改变。
- 顶栏在窄屏可折行或收起次要标签，但项目名、返回、保存状态和主要动作必须可达。
- 画布仍占满剩余视口，不引入页面级滚动条。
